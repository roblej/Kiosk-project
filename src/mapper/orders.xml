<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orders">
    <select id="status" resultType="vo.OrdersVO">
        select * from orders <!-- 오름차순 -->
    </select>

    <update id="update" parameterType="vo.OrdersVO">
        UPDATE orders
        SET o_status = #{o_status}
        WHERE o_idx = #{o_idx}
    </update>
</mapper>
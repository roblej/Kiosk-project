<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orders">

    <select id="status" resultType="vo.order_VO">
        select * from orders ORDER BY o_idx DESC
    </select>

    <update id="update" parameterType="vo.order_VO">
        UPDATE orders
        SET o_status = #{o_status}
        WHERE o_idx = #{o_idx}
    </update>

    <insert id="addorders" parameterType="map">
        INSERT INTO orders ( o_idx, o_number, o_total_amount, o_status, o_is_takeout, user_id )
        VALUES ( #{o_idx}, #{o_number}, #{o_total_amount}, #{o_status}, #{o_is_takeout}, #{user_id} )
    </insert>

</mapper>